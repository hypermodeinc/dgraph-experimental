from setuptools import setup, find_packages

setup(
    name="hypkit",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="A test package with hello() function",
)